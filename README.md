# เกมสุ่มเปิดซองแดงอั่งเปาจีน (Red Envelope Game)

เกมสุ่มเปิดซองแดงอั่งเปาจีนเป็น web application ที่ให้ผู้เล่นสามารถกำหนดจำนวนซองแดงและช่วงเงินรางวัล จากนั้นคลิกเลือกซองแดงเพื่อเปิดและรับเงินรางวัลแบบสุ่ม

## คุณสมบัติ

- 🧧 กำหนดจำนวนซองแดงและช่วงเงินรางวัลได้
- 🐴 ซองแดงแต่ละใบมีรูปม้าการ์ตูนน่ารักที่แตกต่างกัน
- 🎯 คลิกเพื่อเปิดซองและรับเงินรางวัลแบบสุ่ม
- 📊 แสดงสถิติการเล่นแบบเรียลไทม์
- 🎨 ออกแบบ UI ที่สวยงามและใช้งานง่าย
- 📱 รองรับการใช้งานบนอุปกรณ์มือถือ

## โครงสร้างไฟล์

```
red-envelope-game/
├── index.html          # หน้าเว็บหลัก
├── styles.css          # ไฟล์ CSS สำหรับการจัดแต่งหน้าตา
├── game.js             # ไฟล์ JavaScript หลักของเกม
├── game.test.js        # ไฟล์ทดสอบ
├── jest.setup.js       # การตั้งค่า Jest
├── package.json        # การตั้งค่า Node.js และ dependencies
└── README.md           # เอกสารนี้
```

## การติดตั้งและรันโปรเจค

### ข้อกำหนดเบื้องต้น
- Node.js (เวอร์ชัน 14 หรือใหม่กว่า)
- npm (มาพร้อมกับ Node.js)

### การติดตั้ง

1. Clone หรือดาวน์โหลดโปรเจค
2. เปิด terminal ในโฟลเดอร์โปรเจค
3. รันคำสั่งติดตั้ง dependencies:

```bash
npm install
```

### การรันเกม

เปิดไฟล์ `index.html` ในเว็บเบราว์เซอร์ หรือใช้ web server เช่น:

```bash
# ใช้ Python (ถ้ามี)
python -m http.server 8000

# ใช้ Node.js http-server (ต้องติดตั้งก่อน)
npx http-server
```

### การรันการทดสอบ

```bash
# รันการทดสอบครั้งเดียว
npm test

# รันการทดสอบแบบ watch mode
npm run test:watch

# รันการทดสอบพร้อม coverage report
npm run test:coverage
```

## เทคโนโลยีที่ใช้

- **HTML5**: โครงสร้างหน้าเว็บ
- **CSS3**: การจัดแต่งหน้าตาและ animations
- **JavaScript (ES6+)**: ตรรกะของเกมและการจัดการ DOM
- **Jest**: Framework สำหรับการทดสอบ unit tests
- **fast-check**: Library สำหรับ property-based testing
- **jsdom**: สภาพแวดล้อมการทดสอบสำหรับ DOM

## การทดสอบ

โปรเจคนี้ใช้การทดสอบแบบ dual approach:

### Unit Testing
- ทดสอบฟังก์ชันแต่ละตัวทำงานถูกต้อง
- ทดสอบกรณีขอบเขตและการจัดการข้อผิดพลาด
- ทดสอบการทำงานร่วมกันระหว่าง components

### Property-Based Testing
- ทดสอบคุณสมบัติที่ควรเป็นจริงในทุกสถานการณ์
- ใช้ fast-check ในการสร้างข้อมูลทดสอบแบบสุ่ม
- ตรวจสอบความถูกต้องโดยรวมของระบบ

## โครงสร้างโค้ด

### Modules หลัก

- **GameState**: จัดการสถานะเกมหลัก
- **SettingsManager**: จัดการและตรวจสอบการตั้งค่าเกม
- **EnvelopeGenerator**: สร้างและจัดการซองแดงพร้อมรูปม้าการ์ตูน
- **PrizeCalculator**: คำนวณเงินรางวัลแบบสุ่ม
- **UIManager**: จัดการการแสดงผลและการโต้ตอบกับผู้ใช้
- **GameController**: ควบคุมการไหลของเกมและตรรกะหลัก

## การพัฒนาต่อ

โปรเจคนี้ถูกออกแบบให้ขยายได้ง่าย สามารถเพิ่มคุณสมบัติใหม่ได้ เช่น:

- ระบบเสียงและ animations
- การบันทึกคะแนนสูงสุด
- โหมดเกมหลากหลาย
- การแชร์ผลลัพธ์

## License

MIT License - ดูรายละเอียดในไฟล์ LICENSE